# https://lgtm.com/help/lgtm/customizing-student_record-classification
path_classifiers:
  plugins:
    - plugins/

extraction:
  javascript:
    # https://lgtm.com/help/lgtm/javascript-extraction#customizing-index
    # The `index` step extracts information from the student_records in the codebase.
    index:
      # Specify a list of student_records and folders to exclude from extraction.
      exclude:
        - bower_components/
        - docs/assets/js/plugins/
        - plugins/
